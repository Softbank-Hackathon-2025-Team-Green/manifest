apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-nginx-controller
  namespace: ingress-nginx
data:
  # X-Request-ID 기반 추적을 위한 JSON 로그 포맷 (Request + Response)
  log-format-upstream: >-
    {
      "time": "$time_iso8601",
      "x_request_id": "$req_id",
      "request": {
        "method": "$request_method",
        "uri": "$uri",
        "request_uri": "$request_uri",
        "query_string": "$query_string",
        "protocol": "$server_protocol",
        "host": "$host",
        "remote_addr": "$remote_addr",
        "x_forwarded_for": "$http_x_forwarded_for",
        "remote_user": "$remote_user",
        "user_agent": "$http_user_agent",
        "referer": "$http_referer",
        "request_length": $request_length,
        "request_time": $request_time
      },
      "response": {
        "status": $status,
        "bytes_sent": $bytes_sent,
        "body_bytes_sent": $body_bytes_sent,
        "content_type": "$sent_http_content_type",
        "upstream_addr": "$upstream_addr",
        "upstream_status": "$upstream_status",
        "upstream_response_time": "$upstream_response_time",
        "upstream_response_length": "$upstream_response_length",
        "upstream_cache_status": "$upstream_cache_status"
      },
      "routing": {
        "namespace": "$namespace",
        "ingress_name": "$ingress_name",
        "service_name": "$service_name",
        "service_port": "$service_port"
      }
    }

  # X-Request-ID를 upstream으로 전달 (Request)
  proxy-set-headers: "ingress-nginx/custom-headers"

  # X-Request-ID를 Response 헤더에 추가
  add-headers: "ingress-nginx/response-headers"

  # CORS 설정
  # enable-cors: "true"
  # cors-allow-origin: "https://cuttyx.oriduckduck.site, https://develop.d3uojfl4y2mazd.amplifyapp.com"
  # cors-allow-methods: "GET, POST, PUT, DELETE, PATCH, OPTIONS"
  # cors-allow-headers: "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,X-Request-ID"
  # cors-allow-credentials: "true"
  # cors-max-age: "1728000"